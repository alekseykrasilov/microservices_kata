server:
  port: 8083

spring:
  application:
    name: client-service

eureka:
  client:
    serviceUrl:
      defaultZone: ${EUREKA_URI:http://localhost:8761/eureka}
    instance:
      preferIpAddress: true

book-service:
  url: http://localhost:8081/